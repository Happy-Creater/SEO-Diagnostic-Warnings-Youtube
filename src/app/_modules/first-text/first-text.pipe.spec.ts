import { FirstTextPipe } from './first-text.pipe';

describe('FirstTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstTextPipe();
    expect(pipe).toBeTruthy();
  });
});
