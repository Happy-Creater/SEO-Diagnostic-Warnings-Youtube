import { CheckboxChildDirective } from './checkbox-child.directive';

describe('CheckboxChildDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new CheckboxChildDirective();
  //   expect(directive).toBeTruthy();
  // });
});
