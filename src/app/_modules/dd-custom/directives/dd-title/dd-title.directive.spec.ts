import { DDTitleDirective } from './dd-title.directive';

describe('DdTitleDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new DDTitleDirective();
  //   expect(directive).toBeTruthy();
  // });
});
