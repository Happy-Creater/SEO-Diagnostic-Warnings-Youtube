import { CheckboxControllerDirective } from './checkbox-controller.directive';

describe('CheckboxControllerDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new CheckboxControllerDirective();
  //   expect(directive).toBeTruthy();
  // });
});
