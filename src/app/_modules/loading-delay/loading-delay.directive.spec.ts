import { LoadingDelayDirective } from './loading-delay.directive';

describe('LoadingDelayDirective', () => {
  it('should create an instance', () => {
    // const directive = new LoadingDelayDirective();
    // expect(directive).toBeTruthy();
  });
});
