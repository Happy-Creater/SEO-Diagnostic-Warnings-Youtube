import { TextboxClearableDirective } from './textbox-clearable.directive';

describe('TextboxClearableDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new TextboxClearableDirective();
  //   expect(directive).toBeTruthy();
  // });
});
