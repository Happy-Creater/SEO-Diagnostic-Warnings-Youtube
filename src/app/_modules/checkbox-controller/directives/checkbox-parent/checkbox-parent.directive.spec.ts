import { CheckboxParentDirective } from './checkbox-parent.directive';

describe('CheckboxParentDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new CheckboxParentDirective();
  //   expect(directive).toBeTruthy();
  // });
});
