import { DDListDirective } from './dd-list.directive';

describe('DdListDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new DDListDirective();
  //   expect(directive).toBeTruthy();
  // });
});
