import { MenuLeftDirective } from './menu-left.directive';

describe('MenuLeftDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new MenuLeftDirective();
  //   expect(directive).toBeTruthy();
  // });
});
