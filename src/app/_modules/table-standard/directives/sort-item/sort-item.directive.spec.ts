import { SortItemDirective } from './sort-item.directive';

describe('SortItemDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new SortItemDirective();
  //   expect(directive).toBeTruthy();
  // });
});
