import { MappingTranslatePipe } from './mapping-translate.pipe';

describe('MappingTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new MappingTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
