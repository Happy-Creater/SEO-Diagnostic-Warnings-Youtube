import { TextEvolutionPipe } from './text-evolution.pipe';

describe('TextEvolutionPipe', () => {
  it('create an instance', () => {
    const pipe = new TextEvolutionPipe();
    expect(pipe).toBeTruthy();
  });
});
