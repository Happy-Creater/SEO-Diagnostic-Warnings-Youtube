import { BoxLoadingDirective } from './box-loading.directive';

describe('BoxLoadingDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new BoxLoadingDirective();
  //   expect(directive).toBeTruthy();
  // });
});
